<Window x:Class="RobloxMultiLauncher.Dialogs.AddAccountDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Account" Height="580" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1e1e1e"
        Foreground="#cccccc"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style x:Key="Lbl" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#cccccc" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Margin" Value="0,0,0,6" />
        </Style>

        <Style x:Key="Input" TargetType="Control">
            <Setter Property="Background" Value="#252526" />
            <Setter Property="Foreground" Value="#ffffff" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#3c3c3c" />
            <Setter Property="Padding" Value="12,10" />
            <Setter Property="FontSize" Value="13" />
        </Style>

        <Style x:Key="BtnPrimary" TargetType="Button">
            <Setter Property="Background" Value="#0e639c"/>
            <Setter Property="Foreground" Value="#ffffff"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#1177bb"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnSecondary" TargetType="Button">
            <Setter Property="Background" Value="#2d2d2d"/>
            <Setter Property="Foreground" Value="#cccccc"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#3c3c3c"/>
            <Setter Property="Padding" Value="0,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#3c3c3c"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <StackPanel Margin="32">
        <TextBlock Text="+ Add Roblox Account" FontSize="18" Foreground="#ffffff" Margin="0,0,0,24"/>

        <!-- Display Name -->
        <TextBlock Text="Display Username" Style="{StaticResource Lbl}"/>
        <TextBox x:Name="TxtUsername" Style="{StaticResource Input}" Margin="0,0,0,16"/>

        <!-- Cookie -->
        <Grid Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Roblox Cookie (.ROBLOSECURITY)" Style="{StaticResource Lbl}" Margin="0"/>
            <Button Grid.Column="1" Content="ℹ️ How to get?" Background="Transparent" Foreground="#0e639c" BorderThickness="0" FontSize="11" Cursor="Hand" Click="HelpCookie_Click"/>
        </Grid>
        <PasswordBox x:Name="PbCookie" Style="{StaticResource Input}" Margin="0,0,0,6"/>
        <TextBlock Text="Should start with _|WARNING:-DO-NOT-SHARE-THIS..." FontSize="10" Foreground="#858585" Margin="0,0,0,20"/>

        <!-- Place ID -->
        <TextBlock Text="Place ID (Game ID)" Style="{StaticResource Lbl}"/>
        <TextBox x:Name="TxtPlaceId" Style="{StaticResource Input}" Margin="0,0,0,16"/>

        <!-- Private Server -->
        <TextBlock Text="Private Server Link / Code (Optional)" Style="{StaticResource Lbl}"/>
        <TextBox x:Name="TxtPrivateLink" Style="{StaticResource Input}" Margin="0,0,0,32"/>

        <Grid Margin="0,30,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Button Grid.Column="0" Content="Cancel" Style="{StaticResource BtnSecondary}" Click="Cancel_Click"/>
            <Button Grid.Column="2" Content="Add Account" Style="{StaticResource BtnPrimary}" Click="Add_Click"/>
        </Grid>
    </StackPanel>
</Window>
